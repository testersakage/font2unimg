# font2unimg
TrueTypeフォントをPNG画像に変換する

MSゴシック等のTrueTypeフォント（固定幅）をLuanti/Minecloniaに合わせた大きさ(12px x 12px)のPNG画像ファイルに変換します。
画像ファイルは一枚あたり文字が32列✕8行で256枚生成されます。

注意：生成されたPNG画像はそのままではLuantiが対応していないので利用できません。

コードの生成にCpilotを利用しています。
